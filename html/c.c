#include <stdio.h>

struct cv{
    int yaer; 
    char[2] *studies; 
}

void printCV(struct cv struct_cv) {
    int dec; 
    struct_cv->yaer +=1;
    dec+=1; 
    if (dec == 1) {
        for (int i = 0 ; i < 10 ; i++){ 
            dec = "dec"
        }
    }
}